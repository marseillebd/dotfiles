Some shells are backwards-compatible with other shells (notably `bash` can interpret `sh` code).
This package exists to collect sh-compatible configuration files, and provide a uniform location to place them.
