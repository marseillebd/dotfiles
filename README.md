# Stow

Hop into `via-stow/` and run `stow -v <packages...>`, usually.
For xfce, use `stow -v --adopt xfce`, since its configs are awful.
You could also use `--adopt` to pull in dotfiles from other sources and use git to compare them.

# NixOS

I've still got the `via-nix/` folder for reference to what packages I had installed on my NixOS.
